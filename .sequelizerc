const path = require("path");

module.exports = {
  config: path.resolve("src/db/config", "config.json"),
  "models-path": path.resolve("src/db", "sequelize"),
  "migrations-path": path.resolve("src/db", "migrations"),
  "seeders-path": path.resolve("src/db", "seeders"),
};
